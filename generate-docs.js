const fs = require('fs');
const path = require('path');

// عنوان و مشخصات اصلی داکیومنت آرکاورس
const docContent = `# 📋 مستندات فاز ۱: راه‌اندازی، کانفیگ و زیرساخت انیمیشن

> **نام پروژه:** آرکاوِرس (ArkaVerse.ir)  
> **آخرین بروزرسانی:** ${new Date().toLocaleDateString('fa-IR')}  
> **وضعیت فاز ۱:** تکمیل شده و پایدار ✅

---

## 🚀 ۱. مشخصات و پالت رنگی اختصاصی (Tailwind CSS v4)
پالت رنگی نئونی و کهکشانی مجموعه آرکاورس در فایل \`app/globals.css\` با ساختار استاندارد لایه تم تزریق شده است:
* 🌌 **ArkaBg (#0B0F19):** مشکی عمیق کهکشانی برای پس‌زمینه اصلی صفحات.
* 🌐 **ArkaCyan (#00F2FE):** آبی نئون هوشمند برای هدر، دکمه‌ها و بخش مکانیزاسیون.
* 🟢 **ArkaGreen (#05CD99):** سبز زمردی درخشان برای بخش نظام‌مندی و شاخص‌های رشد.
* 🪙 **ArkaText (#E2E8F0):** نقره‌ای/سفید مات برای خوانایی سئوی متون و متون بدنه.

---

## 🛠 ۲. ساختار درختی پوشه‌ها و ساختار منطقی (Folder Structure)
پروژه بر پایه معماری **Next.js App Router** و سیستم **Route Groups** تفکیک شده است:

\`\`\`text
arkaverse/
├── app/
│   ├── (marketing)/      # فضا و صفحات اصلی مارکتینگ سایت (صفحه اصلی، درباره ما و...)
│   │   └── page.tsx      # صفحه اصلی شامل منوی بالا، تایتل کاری و انیمیشن ورودی
│   ├── (dashboard)/      # فضای رزرو شده برای توسعه پنل مدیریت آینده آرکاورس
│   ├── globals.css       # فایل استایل ریشه و محل ثبت پالت رنگی نئونی
│   └── layout.tsx        # لایوت ریشه (تنظیم زبان fa، جهت‌نویسی RTL و فونت سیستم مک)
├── docs/                 # 📂 پوشه اختصاصی مستندات تفکیک شده پروژه
│   └── phase-01-setup.md # 📄 مستندات همین مرحله
├── lib/
│   └── gsap.ts           # فایل کانفیگ و رجیستر بومی انیمیشن‌های کلاینت
└── generate-docs.js      # اسکریپت خودکار تولید مستندات پروژه
\`\`\`

---

## 🎬 ۳. زیرساخت انیمیشن‌ها (GSAP & UI Component)
* کتابخانه‌های \`gsap\` و \`@gsap/react\` به صورت کاملاً ایزوله نصب شدند.
* اولین انیمیشن ورودی لوکس کلاینت (Timeline Animation) روی منوها و تایتل کاری اصلی وسط صفحه با موفقیت فعال شد.

---
_این فایل به صورت خودکار توسط سیستم تولید داکیومنت آرکاورس در پوشه مستندات ذخیره شده است._
`;

// ۱. ساخت پوشه docs در صورت عدم وجود
const docsDir = path.join(__dirname, 'docs');
if (!fs.existsSync(docsDir)){
    fs.mkdirSync(docsDir);
}

// ۲. مسیر ساخت فایل داکیومنت نهایی درون پوشه docs
const outputPath = path.join(docsDir, 'phase-01-setup.md');

try {
  fs.writeFileSync(outputPath, docContent, 'utf8');
  // حذف فایل قدیمی در ریشه در صورت وجود (برای تمیزکاری)
  const oldFile = path.join(__dirname, 'ARKA_DOCUMENTATION.md');
  if (fs.existsSync(oldFile)) {
    fs.unlinkSync(oldFile);
  }
  console.log('✅ پوشه docs ایجاد شد و داکیومنت فاز ۱ (phase-01-setup.md) درون آن ذخیره گردید!');
} catch (err) {
  console.error('❌ خطا در تولید داکیومنت:', err);
}
